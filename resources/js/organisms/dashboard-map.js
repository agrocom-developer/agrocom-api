/**
 * organisms/dashboard-map.js — placeholder. El tab "Mapa" del dashboard
 * (dashboard.blade.php) todavía no renderiza ningún [data-ag-map]; este
 * módulo solo existe para que el import() dinámico de app.js resuelva
 * durante el build. Se completa con Leaflet + Esri World Imagery cuando se
 * implemente ese tab.
 */
